post:
  tags:
    - sessions
  summary: Sign In
  operationId: signIn
  requestBody:
    description: Sign In data
    content:
      application/json:
        schema:
          $ref: "../openapi.yaml#/components/schemas/Login"
  responses:
    '200':
      description: SUCCESS
