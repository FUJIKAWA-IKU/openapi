get:
  tags:
    - mypage
  description:
    マイページの情報を取得する
  responses:
    '200':
      description: SUCCESS
      content:
        application/json:
          schema:
            $ref: "../openapi.yaml#/components/schemas/UserProfile"
put:
  tags:
    - mypage
  description:
    マイページの情報を更新する
  responses:
    '200':
      description: SUCCESS
      content:
        application/json:
          schema:
            $ref: '../openapi.yaml#/components/schemas/UserProfile'